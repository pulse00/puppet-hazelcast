# /etc/init/hazelcast.conf

description "hazelcast"
author      "Robert Gruendler <robert.gruendler@dubture.com>"

start on vagrant-mounted
stop on shutdown

chdir /opt/hazelcast-<%= @version %>/bin

script
    exec sh /opt/hazelcast-<%= @version %>/bin/server.sh
end script
